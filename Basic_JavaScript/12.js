// boolenas & comparison operator

    //booleans 
        // true,false

        let num1 = "10";
        let num2 = 10;
        // let num2 = 8;
        // console.log(num1<=num2);

    // == vs === ; 
        // console.log(num1 == num2); // "==" check only values are equal or not; it can't check datatypes of variable ,
        // // if values r equal it assign true else it assigns false, it happens only in JS not any other Lang.
        // console.log(num1 === num2); // "===" check values with datatypes , here both values and dataypes should be equal; for true

    // != vs !== ((shift + 1) + =) 
    console.log (num1 !== num2); // same as above
